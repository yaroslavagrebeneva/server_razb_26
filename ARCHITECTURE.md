# Архитектура проекта CheckIn

## 1. Общая архитектура

CheckIn построен по клиент-серверной архитектуре.

Система состоит из следующих основных контейнеров:

- **Web Client** — веб-интерфейс системы;
- **Backend Service** — REST API и бизнес-логика;
- **Database** — MySQL, хранение основных данных;
- **File Storage** — хранение документов и файлов, прикрепляемых к объяснениям отсутствия.

Пользователи взаимодействуют с системой через Web Client. Web Client отправляет запросы в Backend Service по REST API. Backend Service выполняет бизнес-логику, проверяет права доступа и взаимодействует с MySQL и файловым хранилищем.

---

## 2. Пользователи системы

В системе предусмотрено пять основных ролей:

- **Студент** — отмечает посещаемость через QR-код и предоставляет объяснения отсутствия;
- **Староста** — может вручную отмечать посещаемость группы, если QR-код невозможно использовать или студент пришёл позднее;
- **Преподаватель** — создаёт занятия и QR-сессии, контролирует посещаемость и проверяет объяснения;
- **Куратор** — контролирует посещаемость закреплённых групп и получает уведомления о низкой посещаемости;
- **Декан** — получает сводные данные и формирует отчёты.

---

## 3. Use Cases

### UC-01. Отметка посещаемости через QR-код

**Актор:** Студент

**Предусловия:**

- студент авторизован;
- преподаватель создал занятие;
- QR-сессия активна.

**Основной сценарий:**

1. Преподаватель создаёт занятие.
2. Система создаёт QR-сессию.
3. Преподаватель отображает QR-код.
4. Студент сканирует QR-код.
5. Backend проверяет действительность QR-сессии.
6. Система создаёт запись посещаемости.
7. В записи сохраняются студент, занятие, способ отметки и время.
8. Посещаемость отображается в системе.

**Результат:** Студент отмечен как присутствующий.

---

### UC-02. Ручная отметка посещаемости

**Актор:** Староста

**Предусловия:**

- староста авторизован;
- занятие существует;
- QR-код невозможно использовать либо требуется отметить опоздавшего студента.

**Основной сценарий:**

1. Староста открывает список группы.
2. Система отображает студентов группы.
3. Староста отмечает присутствующего студента.
4. При необходимости указывает статус опоздания или отсутствия.
5. Система сохраняет запись посещаемости.
6. В записи сохраняется способ отметки `manual`.
7. В поле `marked_by` сохраняется пользователь, выполнивший отметку.

**Результат:** Посещаемость группы сохранена с указанием автора отметки.

---

### UC-03. Добавление объяснения отсутствия

**Актор:** Студент

**Предусловия:**

- студент авторизован;
- занятие существует.

**Основной сценарий:**

1. Студент открывает список пропусков.
2. Выбирает занятие.
3. Выбирает тип причины.
4. Добавляет описание.
5. При необходимости загружает документ.
6. Система создаёт объяснение со статусом `pending`.
7. Преподаватель получает информацию о новом объяснении.

**Результат:** Объяснение отправлено преподавателю.

---

### UC-04. Проверка объяснения

**Актор:** Преподаватель

**Предусловия:**

- преподаватель авторизован;
- существует объяснение со статусом `pending`.

**Основной сценарий:**

1. Преподаватель открывает список объяснений.
2. Выбирает объяснение студента.
3. Просматривает текст причины.
4. Просматривает прикреплённые документы.
5. При необходимости добавляет комментарий.
6. Принимает или отклоняет объяснение.
7. Система сохраняет результат проверки.
8. Студент получает уведомление о результате.

**Результат:** Объяснение получает статус `accepted` или `rejected`.

---

### UC-05. Контроль посещаемости куратором

**Актор:** Куратор

**Предусловия:**

- куратор авторизован;
- у куратора есть закреплённые группы.

**Основной сценарий:**

1. Куратор открывает список закреплённых групп.
2. Система рассчитывает показатели посещаемости.
3. При обнаружении низкой посещаемости создаётся уведомление.
4. Куратор открывает уведомление.
5. Просматривает проблемную группу или студента.

**Результат:** Куратор получает актуальную информацию о проблемах с посещаемостью.

---

### UC-06. Формирование отчёта

**Актор:** Декан

**Предусловия:**

- декан авторизован.

**Основной сценарий:**

1. Декан выбирает факультет.
2. Выбирает кафедру, группу или предмет.
3. Система получает данные о посещаемости.
4. Система рассчитывает показатели.
5. Формируется отчёт.
6. Декан может просмотреть или сохранить отчёт.

**Результат:** Получен сводный отчёт по посещаемости.

---

## 4. REST API Specs

Все методы используют префикс `/api/v1`.

| Метод | URI | Описание | Доступ (роль) | Ожидаемый код |
|---|---|---|---|---|
| POST | `/api/v1/auth/login` | Вход в систему и получение JWT-токена | Все | 200 OK |
| POST | `/api/v1/auth/register` | Регистрация пользователя | Все | 201 Created |
| GET | `/api/v1/users/me` | Получение профиля текущего пользователя | Авторизованный | 200 OK |
| POST | `/api/v1/lessons` | Создание занятия | Преподаватель | 201 Created |
| GET | `/api/v1/lessons` | Получение списка занятий | Авторизованный | 200 OK |
| POST | `/api/v1/lessons/{lessonId}/qr-sessions` | Создание QR-сессии занятия | Преподаватель | 201 Created |
| POST | `/api/v1/qr-sessions/{sessionId}/check-in` | Отметка студента через QR-код | Студент | 201 Created |
| GET | `/api/v1/lessons/{lessonId}/attendance` | Получение посещаемости занятия | Преподаватель, Староста | 200 OK |
| POST | `/api/v1/lessons/{lessonId}/attendance` | Ручная отметка посещаемости | Староста | 201 Created |
| PATCH | `/api/v1/attendance/{attendanceId}` | Изменение записи посещаемости | Преподаватель, Староста | 200 OK |
| POST | `/api/v1/explanations` | Добавление объяснения отсутствия | Студент | 201 Created |
| GET | `/api/v1/explanations` | Получение списка объяснений | Преподаватель | 200 OK |
| PATCH | `/api/v1/explanations/{explanationId}` | Принятие или отклонение объяснения | Преподаватель | 200 OK |
| GET | `/api/v1/notifications` | Получение уведомлений | Авторизованный | 200 OK |
| GET | `/api/v1/curator/attendance-alerts` | Получение уведомлений о низкой посещаемости | Куратор | 200 OK |
| GET | `/api/v1/reports/attendance` | Формирование сводного отчёта | Декан | 200 OK |

### Правила API

- Для защищённых методов используется `Authorization: Bearer <JWT>`.
- Идентификаторы сущностей передаются в формате UUID.
- Даты и время передаются в формате ISO 8601.
- Ошибки API возвращаются в едином формате `ErrorResponse`.
- Для ручной отметки система сохраняет пользователя, выполнившего действие, в поле `marked_by`.
- Для отметки через QR-код значение `method` равно `qr`.
- Для ручной отметки значение `method` равно `manual`.

---

## 5. C4 Container Diagram

```plantuml
@startuml
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

title C4 Container Diagram - CheckIn

Person(student, "Студент", "Отмечает посещаемость и предоставляет объяснения")
Person(starosta, "Староста", "Вручную отмечает посещаемость группы")
Person(teacher, "Преподаватель", "Создаёт занятия и QR-коды, контролирует посещаемость")
Person(curator, "Куратор", "Контролирует посещаемость закреплённых групп")
Person(dean, "Декан", "Получает сводные данные и отчёты")

System_Boundary(checkin, "CheckIn") {

    Container(web, "Web Client", "React", "Веб-интерфейс системы")

    Container(api, "Backend Service", "FastAPI", "REST API и бизнес-логика системы")

    ContainerDb(db, "Database", "MySQL", "Данные пользователей, групп, занятий, посещаемости и уведомлений")

    Container(files, "File Storage", "File Storage", "Хранение документов и файлов")
}

Rel(student, web, "Использует")
Rel(starosta, web, "Использует")
Rel(teacher, web, "Использует")
Rel(curator, web, "Использует")
Rel(dean, web, "Использует")

Rel(web, api, "REST API", "HTTPS")
Rel(api, db, "Читает и изменяет данные", "SQL")
Rel(api, files, "Загружает и получает файлы")

@enduml
```

---

## 6. Основные сущности системы

В базе данных используются, в частности, следующие сущности:

1. `users` — пользователи системы;
2. `roles` — роли пользователей;
3. `groups` — учебные группы;
4. `students` — данные студентов;
5. `lessons` — занятия;
6. `qr_sessions` — QR-сессии;
7. `attendance` — записи посещаемости;
8. `explanations` — объяснения отсутствия;
9. `files` — прикреплённые документы;
10. `notifications` — уведомления.

Связи между сущностями проектируются с учётом нормализации данных до третьей нормальной формы (3NF).

---

## 7. Обработка ошибок

Основные HTTP-коды API:

- `200 OK` — запрос успешно выполнен;
- `201 Created` — ресурс успешно создан;
- `204 No Content` — операция выполнена без тела ответа;
- `400 Bad Request` — ошибка валидации данных;
- `401 Unauthorized` — пользователь не авторизован;
- `403 Forbidden` — недостаточно прав;
- `404 Not Found` — ресурс не найден;
- `409 Conflict` — конфликт данных;
- `422 Unprocessable Entity` — ошибка валидации входных данных;
- `500 Internal Server Error` — внутренняя ошибка сервера.

---

## 8. Спецификация OpenAPI

Полная спецификация REST API находится в файле `openapi.yaml`.

Файл содержит:

- описание API CheckIn;
- методы авторизации;
- методы работы с занятиями;
- QR-сессии;
- отметки посещаемости;
- объяснения отсутствия;
- уведомления;
- отчёты;
- схемы запросов и ответов;
- JWT Bearer Authentication;
- описание HTTP-кодов ошибок.
